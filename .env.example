# (선택) Supabase 연결 정보 재정의
# 기본 프로젝트 정보는 코드(src/lib/supabase.ts)에 이미 들어 있어 이 파일 없이도 동작합니다.
# 다른 Supabase 프로젝트로 바꾸고 싶을 때만 .env 로 복사해 값을 채우세요.
# 프로젝트 → Settings → API 에서 값을 복사합니다.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
